# ── Copy this file to .env and fill in your values ──────────────────────────

# Railway app WebSocket URL (use wss:// for production)
RAILWAY_URL=wss://your-app.up.railway.app

# Must match PRINTER_BRIDGE_SECRET in your Railway environment variables
PRINTER_BRIDGE_SECRET=change_me_to_a_strong_random_secret

# Windows COM port for USB thermal printer
# Find in: Device Manager → Ports (COM & LPT) → USB Serial Port
# Examples: //./COM3  //./COM4  //./COM5
PRINTER_INTERFACE=//./COM3
